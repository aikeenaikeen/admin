# Для запуска через infra/nginx оставь пустым (используется текущий домен).
VITE_API_BASE_URL=
VITE_RECOGNITION_STREAM_URL=

# Для dev без nginx (когда backend/recognition слушают порты на хосте):
# VITE_API_BASE_URL=http://localhost:3000
# VITE_RECOGNITION_STREAM_URL=http://localhost:5001